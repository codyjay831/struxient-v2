/**
 * Guard: Timezone Canonicalization
 * 
 * Rule: All schedule truth must be stored in UTC/Canonical timezone.
 */
console.log('🔍 Running guard_schedule_timezone_canonicalization...');

// TODO Phase B/C Implementation:
// 1. Verify that all DateTime fields in schedule-related models are stored as UTC.
// 2. Scan UI layers to ensure time conversions are display-only.

console.log('⚠️ NOT YET ENFORCED: Timezone canonicalization will be active after schema migration.');
process.exit(0);
