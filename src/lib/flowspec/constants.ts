/**
 * FlowSpec Engine Constants
 */

/**
 * Maximum number of iterations allowed for a single Node.
 * This prevents infinite loops in remediation cycles.
 */
export const MAX_NODE_ITERATIONS = 100;
